mod block;
use block::BlockHeader;
use risc0_zkvm::prove::Prover;

fn main() {
    // init block header to be verified
    let block: BlockHeader;

    // 1. Define a Prover
    // let prover = Prover::new();
}